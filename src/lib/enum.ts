export enum MsgType {
  Text = 1,
  Button = 2,
  Image = 3,
  Video = 4,
  CardGroup = 5,
  ImgGroup = 6,
}

export enum OrderType {
  descending = 'desc',
  ascending = 'asc',
}

export enum BotType {
  FB = 'fb',
  LINE = 'line',
}
